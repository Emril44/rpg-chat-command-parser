command = { "/" ~ verb ~ (whitespace ~ target)? ~ (whitespace ~ flag)* }
verb    = { ASCII_ALPHANUMERIC+ }
target  = { ASCII_ALPHANUMERIC+ }
flag = { "--" ~ key ~ "=" ~ value }
key  = { ASCII_ALPHANUMERIC+ }
value = { ASCII_ALPHANUMERIC+ }
whitespace = _{ " " }
